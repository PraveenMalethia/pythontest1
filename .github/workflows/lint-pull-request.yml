name: Main Workflow

on:
  workflow_call:

jobs:
  lint_python:
    name: Lint Python Code
    runs-on: ubuntu-latest

    steps:
    - name: Use Python Linting Workflow
      uses: ./.github/workflows/lint_python.yml
